{
  "name": "h2e-offchain",
  "private": true,
  "description": "H2E off-chain: indexer, aged-balance engine, round builder, keeper, election API",
  "scripts": {
    "test": "ts-mocha -p tsconfig.json -t 60000 test/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
